import styles from "../app/page.module.css";

const Box = () => {
  return (
    <div className={styles.main}>
      <h2 className={styles["heading-two"]}>This is a Box.</h2>
    </div>
  );
};

export default Box;
